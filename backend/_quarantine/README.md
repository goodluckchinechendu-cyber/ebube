# Quarantined: login OTP

Direct login + email verification replaced login OTP.
These files are kept only for historical reference and are blocked from HTTP.
Live login uses `login.php` + `auth_util.php` (`login_user_payload`).
