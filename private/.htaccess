# Block all HTTP access. Secrets are loaded by PHP from the filesystem only
# (cPanel / SSH). Do not remove this file.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
